"use client";

// Reusing Login Page for simplicity as user requested "Login / Signup"
// In a real app, signup would collect more details.
import LoginPage from "@/app/login/page";

export default function SignupPage() {
    return <LoginPage />;
}
